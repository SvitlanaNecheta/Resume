
  <!--<a href={{href}} {{#if blank}}target="_blank"{{/if}}>{{text}}</a> -->
 
<!--{{#unless noLine}}
  {{#if line}}
  {{else}}
  /
  {{/if}}
{{/unless}}-->
  <div class="nav-item">
      <a href={{href}} class="nav-link active">{{text}}</a>
  </div>                      
            